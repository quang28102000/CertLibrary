export interface CourseRegister {
    employee_id: string;
    course_id: string;
    // platform: string;
    status: number;
    startDate: String;
    endDate: String;
    certLink: string;
    // totalTime:string;
}
